import React from "react";

const heading = {
  color: "red",
  fontSize: "83px"
};

const Inline = () => {
  return <h1 style={heading}>this is just the Start</h1>;
};

export default Inline;
